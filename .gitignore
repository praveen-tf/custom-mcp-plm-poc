.env
__pycache__/
.playwright-cli/
.venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/

# SpecStory conversation transcripts — local-only, not versioned.
.specstory/

# Real sampled data + generated artifacts from the synthetic-data workflow (spec 003).
# Real values must never be committed; the synthetic SQL is regenerated on demand.
pg-mcp-server/scripts/sample_data/
pg-mcp-server/scripts/synthetic_load.sql
